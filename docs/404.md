---
layout: page
title: 404 - Page Not Found
---
# 404 - Page Not Found

<!--
This page is shown in the VitePress site. For GitHub Pages deployed sites
we rely on `docs/public/404.html` which contains an HTML `<meta refresh>` for
better crawler support plus a JS fallback that preserves the original path
and redirects by browser language.
-->

<p>The page you are looking for does not exist. You will be redirected shortly.</p>

<script>
  (function() {
    try {
      const path = window.location.pathname;
      const base = '/Snap.Hutao-Manjusaka.Docs/';

      // Remove base from path
      let cleanPath = path;
      if (cleanPath.startsWith(base)) {
        cleanPath = cleanPath.substring(base.length);
      }

      // If path already starts with a language code, do nothing (show 404)
      const langCodes = ['zh', 'en', 'ru', 'id', 'jp', 'tw'];
      const pathParts = cleanPath.split('/');
      if (langCodes.includes(pathParts[0])) {
        return; // Already in a language path
      }

      // Detect browser language; fall back to zh
      const lang = (navigator.language || navigator.userLanguage || 'zh').split('-')[0];
      const supportedLangs = ['zh', 'en', 'ru', 'id', 'jp', 'tw'];
      const targetLang = supportedLangs.includes(lang) ? lang : 'zh';

      const newPath = base + targetLang + '/' + cleanPath;
      window.location.replace(newPath);
    } catch (e) {
      // Let the page display if something failed
    }
  })();
</script>
# 404 - Page Not Found

The page you are looking for does not exist. Redirecting...